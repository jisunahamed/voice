{
  "name": "voice-agent",
  "version": "1.0.0",
  "description": "Voice agent execution script for Replit",
  "scripts": {
    "start": "poetry install && poetry run uvicorn main:app --host 0.0.0.0 --port 8000"
  }
}
